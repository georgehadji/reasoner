The following is a set of claims made about the correctness and safety of the "Reasoner" codebase (Python 3.12 + FastAPI). Each claim has been written as a confident, affirmative statement — as if documenting a finished, production-ready system.

Your task: treat every claim as a draft answer that requires independent verification. For each claim, generate specific verification questions, then answer them critically to identify where the claim fails, is incomplete, or depends on unverified assumptions.

ARCHITECTURE CONTEXT:
- Hexagonal DDD + CQRS + Event Sourcing + Mixin Composition
- src/reasoner/infrastructure/ — LLM adapters, rate_limiter, token_cache, auth, billing, circuit_breaker, redis/
- src/reasoner/application/ — orchestrator, pipeline.py, event_bus/bus.py, handlers/
- src/reasoner/api/ — SSE streaming, middleware, auth_deps, CSRF, webhooks
- src/reasoner/core/ — settings, constants, search clients

ALREADY CONFIRMED BUGS (do not re-report):
- B-01 to B-12: Rate limiter path, undefined loggers, CSRF, raw_prompt, AuthManager lock, calculator injection, create_task leaks, Redis pool, postgres_store fire-and-forget, WS broadcast
- B-13 to B-15: SSE disconnect, Redis unbounded pool (FIXED), circuit breaker in-memory
- Circuit breaker: wrong attribute name (max_half_open_calls vs half_open_max_calls) — FIXED
- Circuit breaker: StructuredLogger.warning() called with positional args — FIXED

CLAIMS TO VERIFY:

Claim A: "The EventBus is fault-tolerant — if a handler raises an exception, it is caught by _safe_execute() and the bus continues delivering to other handlers without interruption."

Claim B: "The token cache is bounded — it will never consume more than max_entries entries or max_tokens tokens, preventing unbounded memory growth."

Claim C: "The API middleware stack correctly enforces memory limits — if RSS exceeds MEMORY_LIMIT_MB, the server returns HTTP 503 before the request reaches a route handler."

Claim D: "The OpenRouter provider correctly handles all HTTP error responses from the API — rate limit errors (429), authentication errors (401), model not found (404), and server errors (500) are all handled distinctly."

Claim E: "The PipelineOrchestrator's preflight() phase correctly falls back from pipeline to direct_answer when confidence is below the threshold — no pipeline is ever launched unnecessarily."

Claim F: "The run_stream_cached() function correctly caches pipeline results — if the same problem and preset are submitted twice, the second call returns the cached result without invoking any LLM."

Claim G: "The CSRF protection is correctly enforced — every state-changing POST endpoint requires a valid CSRF token, and the token is invalidated after use to prevent replay attacks."

Claim H: "The billing webhook deduplication is atomic — it is impossible for two concurrent workers to both begin processing the same Stripe event simultaneously."

Claim I: "The LLM provider fallback chain is exhaustive — if the primary provider fails, the system always tries the fallback provider before returning a degraded response."

Claim J: "The rate limiter correctly identifies unique clients — two different users behind the same NAT (shared public IP) are not mistakenly throttled together."

For each claim:
1. State 1-2 specific verification questions that would falsify the claim
2. Answer those questions based on the architecture and known patterns
3. Verdict: VERIFIED / PARTIALLY TRUE / FALSE
4. If false or partially true: state the exact failure condition and its severity
