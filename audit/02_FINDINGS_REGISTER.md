# 02 — Findings Register

Confidence scale: 95–100 Verified · 80–94 Highly Likely · 60–79 Probable · <60 Inconclusive.
"2nd-pass ✓" = independently re-verified against source in this audit session. Findings the second pass **refuted** are listed at the bottom and must NOT be implemented.

## Active Findings

| Issue ID | Severity | Confidence | Area | Description | Root Cause | Impact | Supporting Evidence | Contradictory Evidence | Recommended Fix |
|---|---|---|---|---|---|---|---|---|---|
| SEC-01R | HIGH | 100 (2nd-pass ✓) | Security/Ops | Live API keys for ~12 LLM providers + Supabase in local `.env`; weak `CSRF_SECRET=reasoner-csrf-secret-2026`; `CSRF_ENFORCE_BACKEND=false` | Dev convenience config holding production-grade credentials; no secret manager | Any process/tool with workspace read access exfiltrates all provider keys; config catastrophic if promoted to prod | `.env:6-60` (keys), `.env:112` (CSRF secret), `.env:123` (enforce=false). **Corrected:** `git ls-files` shows only `.env.example` tracked; `git check-ignore .env` confirms ignored — NOT committed, no history scrub needed | `.gitignore` correctly excludes it; `.env.example` has no secrets | Rotate keys as precaution; random `CSRF_SECRET` (`secrets.token_urlsafe(32)`); document prod-required values; consider OS keychain/secret manager |
| SEC-02 | HIGH | 100 (2nd-pass ✓) | Security | CSRF cookie set with `httpOnly: false` | Double-submit pattern implemented with JS-readable cookie, though token is already returned in the JSON body | XSS anywhere → CSRF token theft → full CSRF bypass | `ui-next/src/app/api/csrf/route.ts:9-14` (`httpOnly: false`); body already carries token (`NextResponse.json({ token })`, line 8) | Pattern requires client to know token — but body delivery makes cookie readability unnecessary | Set `httpOnly: true`; client keeps using the body token for the `X-CSRF-Token` header |
| SEC-03 | HIGH | 100 | Security | CSRF enforcement disabled via env flag with no prod guard | `CSRF_ENFORCE_BACKEND` short-circuits `require_csrf` | All state-changing endpoints forgeable when flag is false | `src/reasoner/api/auth_deps.py:159-160`; `.env:123` | Documented as CI-intentional in CLAUDE.md (`CSRF_ENFORCE_BACKEND=false` in CI) | Force `true` when `ENVIRONMENT=production` in `core/settings.py` validation |
| SEC-06 | MEDIUM | 85 | Security | CSP allows `'unsafe-inline'` for script/style | Interim CSP (acknowledged in code comment) | CSP gives no XSS defense-in-depth | `src/reasoner/api/middleware.py:28-39` | react-markdown escapes by default; no `dangerouslySetInnerHTML` found | Nonce-based CSP |
| SEC-07 | HIGH | 95 (2nd-pass ✓) | Security | WebSocket bearer token accepted via query param | Convenience fallback in token extraction | Tokens leak to proxy logs, browser history | `src/reasoner/api/routes/websocket.py:17` | Authorization-header path also exists (lines 19-21) | Remove query-param branch; header only |
| SEC-08 | LOW | 70 | Security | Upload/history path handling lacks explicit traversal guard | Relies on UUID IDs rather than path validation | Structural weakness only; IDs are UUIDs today | `infrastructure/uploader.py:56-57`; `api/history.py:52` | All IDs UUID-generated; auth-gated | Add `resolve()`-and-prefix-check helper |
| SEC-09 | MEDIUM | 88 | Security | Production CORS depends entirely on `APP_URL` being set correctly; no startup validation | Single env var, unvalidated | Misconfig → CORS denial-of-service or over-broad origin | `src/reasoner/api/__init__.py:220-224` | Empty list fails closed (safe) | Validate `APP_URL` at startup in production; log allowed origins |
| SEC-10 | HIGH | 100 (2nd-pass ✓) | Security/Deploy | Memory-mode rate limiter + multi-worker guard bypassable via `ENVIRONMENT=development` | RuntimeError raised only when env ≠ development | N workers ⇒ N× the intended rate limit | `src/reasoner/api/__init__.py:91-106` (raise gated at line 100) | Warning message is accurate and loud | Raise whenever `UVICORN_WORKERS>1 and RATE_LIMITER_MODE=="memory"`, env-independent |
| SEC-11 | MEDIUM | 85 | Security/Deploy | Empty `TRUSTED_PROXIES` behind a proxy collapses all clients to proxy IP | Safe-but-silent default | Shared rate-limit bucket for all users behind LB | `src/reasoner/api/client_ip.py:35-50`; `.env.example:188` | XFF parsing itself is correct (rightmost-untrusted walk) | Startup WARNING in production when unset |
| SEC-12 | MEDIUM | 80 | Security | 5xx error logging captures full request headers unfiltered | `extra={"headers": dict(request.headers)...}` | Authorization/cookies land in error store/Sentry | `src/reasoner/api/error_handler.py:193-194` | Internal-only sink; not in responses | Redact `authorization`, `cookie`, `x-api-key`, `x-csrf-token` before logging |
| SEC-13 | MEDIUM | 75 | Security | CSRF secret derived as unsalted SHA-256 of (currently weak) passphrase | Secret treated as passphrase, not random key | Weak passphrase → forgeable tokens if hash leaks | `src/reasoner/api/csrf.py:29-40`; `.env:112` | HMAC usage itself is correct | Require/generate random 32-byte secret (covered by SEC-01R) |
| SEC-15 | MEDIUM | 70 | Security | LLM-originated markdown rendered without explicit `skipHtml` | Reliance on react-markdown defaults | Prompt-injected HTML in model output could render if defaults change/plugins added | SSE → `MarkdownRenderer.tsx` (no `skipHtml`/sanitize prop) | react-markdown escapes HTML by default; no `dangerouslySetInnerHTML` | Add `skipHtml` explicitly |
| SEC-16 | MEDIUM | 75 | Security | Prompt-injection defense is fixed-regex list, trivially bypassed by paraphrase | Pattern-matching approach | Determined injection passes the gate | `src/reasoner/core/sanitization.py:31-50,99-105` | Defense-in-depth exists (LLM-side resilience); perfect prevention impossible | Treat as one layer; add fuzzy/semantic detection if hardening needed |
| SEC-17 | MEDIUM | 85 | Security | Admin compaction endpoint lacks rate limiting | Missing `check_rate_limit` dependency | Leaked admin key ⇒ unlimited expensive compactions | `src/reasoner/api/routes/admin.py:23-42`; contrast `routes/uploads.py:23` | `compare_digest` admin check is sound | Add rate-limit dependency |
| SEC-20 | LOW | 65 | Reliability | Upload dedup hash index is a JSON file with silent write failure | `except OSError: pass` | Duplicate storage after crash; no security impact | `infrastructure/uploader.py:19,32-36` | Dedup is an optimization only | Log failure; consider DB-backed index |
| BC-01 | MEDIUM | 95 (2nd-pass ✓) | Backend core | Dead-letter logging task fire-and-forget; exception never retrieved | `asyncio.create_task` without reference retention or done-callback | Dead-letter entries lost on shutdown; "Task exception was never retrieved" noise | `application/event_bus/bus.py:206`; contrast correct pattern at line 114 (`add_done_callback`) | none found | Retain in set + `add_done_callback(discard)` |
| BC-02 | MEDIUM | 80 | Backend core | Handler list snapshot races with subscribe/unsubscribe | `_handlers` mutated without lock around snapshot | Missed/stale handlers under concurrent subscription churn | `application/event_bus/bus.py:184,214-215` | Subscription churn is rare at runtime | `asyncio.Lock` around subscription mutation |
| BC-03 | HIGH | 90 | Backend core | Critical events use unbounded `await queue.put()` | No timeout/fallback on backpressure | Slow handler can stall the entire pipeline run | `application/event_bus/bus.py:195` | none found | `wait_for(..., 2.0)` + dead-letter on timeout |
| BC-04 | MEDIUM | 75 | Backend core | `debate_rounds`/`phase_costs` mutated without synchronization | Plain dict/list state shared across phase coroutines | Lost/duplicated writes IF phases ever parallelize | `flows/debate_phases.py:52`; `infrastructure/llm/executor.py:411` | WorkflowRunner currently executes phases sequentially — latent, not live | Document invariant or add state mutation lock |
| BC-05 | MEDIUM | 95 (2nd-pass ✓) | Backend core | Prompt-enhancement failure silently falls back, no log, no `state.errors` | Catch-all `except Exception` | Persistent sub-agent failure invisible; silent quality degradation | `application/pipeline.py:296-298` | none found | Log + `state.errors.append` before fallback |
| BC-06 | MEDIUM | 95 (2nd-pass ✓) | Backend core | Same silent-catch on enhancement JSON-parse path | Catch-all `except Exception` | Malformed LLM JSON invisible | `application/pipeline.py:315-316` | none found | Same as BC-05 |
| BC-07 | MEDIUM | 82 | Backend core | Event-handler retries (up to 8s backoff) run inside worker loop, no fail-fast | Uniform retry for all error types | Pathological handler starves event queue | `application/event_bus/bus.py:225-237` | Semaphore (200) bounds concurrency | Fail fast on non-transient errors |
| BC-09 | MEDIUM | 80 | Backend core | `total_cost_usd +=` and `phase_costs[role] =` non-atomic; phase retries can mis-count | Two-step mutation; assignment overwrites on retry | Inaccurate cost reporting on retried phases | `infrastructure/llm/executor.py:409-413` | Runner resets phase state on retry today | Single accumulation method; `+=` for phase_costs too |
| BC-10 | MEDIUM | 80 | Backend core | `drain()` waits on queue join, not in-flight handler completion | `task_done()` semantics vs handler execution | Partial event processing at shutdown | `application/event_bus/bus.py:149-167` | drain timeout caps the hang | Track in-flight task set; gather in drain |
| BC-12 | MEDIUM | 80 | Backend core | Cross-language translation failures logged but not in `state.errors` | Missing error propagation | Downstream phases unaware of degraded input | `application/pipeline.py:395-396,414-415` | none found | Append to `state.errors` |
| BC-19 | LOW | 70 | Backend core | Rebuttal phase assumes `debate_rounds[0]` has `statements` | Missing structure validation | IndexError-class crash on malformed opening | `flows/debate_phases.py:54-64` | Opening phase appends valid rounds normally | Guard + graceful return |
| INF-01 | MEDIUM | 95 (2nd-pass ✓) | Infrastructure | Event-store upsert builds `SET` clause via `', '.join(updates)` f-string | String-built SQL in persistence layer | Not exploitable today (hardcoded field names) but brittle injection-shaped pattern | `infrastructure/persistence/event_store.py:275-281` | Fields hardcoded; values parameterized | Static parameterized SET clause |
| INF-02 | MEDIUM | 100 (2nd-pass ✓) | Infrastructure | Error-store retention DELETE interpolates `days` into SQL | f-string SQL | Safe only because `_safe_int` bounds 1–3650; pattern risk | `infrastructure/persistence/error_store.py:153-160` | Bounds check exists | Bind `f"-{days} days"` as parameter |
| INF-03 | MEDIUM | 90 (2nd-pass ✓) | Infrastructure | WS `send_to_connection`: ref read under lock, used after release; lines 227-230 dead code | TOCTOU between lock release and send | Sends to closed sockets; silent failures | `infrastructure/websocket/manager.py:216-243` | try/except at 239 prevents crash | Re-check state under lock; delete dead code |
| INF-04 | HIGH | 85 | Infrastructure | Snapshot load validates post-snapshot event continuity but not snapshot-version existence | Gap check excludes the boundary | Pruned boundary event ⇒ silently wrong replayed state | `infrastructure/persistence/snapshots.py:197-240` | none found | Validate event exists at snapshot version |
| INF-05 | MEDIUM | 75 | Infrastructure | SQLite transactions use default DEFERRED isolation | No `BEGIN IMMEDIATE` | Theoretical stale-read window under concurrency | `infrastructure/persistence/event_store.py:77-196` | `self._lock` serializes connection access | `BEGIN IMMEDIATE` for write transactions |
| INF-06 | MEDIUM | 85 | Infrastructure | Token-cache `_problem_index` retains stale keys after eviction | Eviction cleans `_entries` but not index lists fully | Lookup performance degrades over long uptime | `infrastructure/token_cache.py:267-292` | `ValueError` handling shows partial awareness | Prune empty/stale index entries on evict |
| INF-08 | HIGH | 85 | Infrastructure | Redis run-state falls back to in-memory silently | `except _RedisUnavailable: pass` paths without logging | Multi-worker cancellation breaks invisibly | `infrastructure/redis/run_state.py:112-150`; partial logging only in `_redis_op` (76-94) | `is_authoritative()` exists but nothing calls attention to it | WARNING per fallback transition + counter; see XS-04 startup check |
| INF-09 | MEDIUM | 80 | Infrastructure | Registry "LRU" eviction is actually FIFO (`next(iter(dict))` / `popitem(last=False)`) | No access-order tracking | Hot circuit-breaker/key state evicted unpredictably | `infrastructure/circuit_breaker.py:477-485`; same pattern `auth_legacy.py:118` | Comment says "oldest" — FIFO may be acceptable if documented | `OrderedDict.move_to_end` on access or document FIFO intent |
| INF-10 | LOW | 70 | Infrastructure | Telemetry `json.dumps(fallback_events)` without `default=` or validation | Unguarded serialization | Non-serializable object ⇒ telemetry row lost | `infrastructure/persistence/telemetry_store.py:135-145` | Works in practice for current event shapes | `json.dumps(..., default=str)` |
| INF-11 | MEDIUM | 70 | Infrastructure | `@retry` wraps circuit-breaker context in Postgres store | Retry/breaker interaction unanalyzed | Confusing breaker state under transient errors | `infrastructure/persistence/postgres_store.py:223-241` | Operations idempotent (unique event_id) | Document idempotency or drop outer retry |
| FE-01 | HIGH | 95 (2nd-pass ✓) | Frontend | `loadMore` uses stale `history` closure: `setHistory([...history, ...next.items])` | Closure capture + dependency-array churn | Lost/duplicated conversations during pagination | `ui-next/src/hooks/useConversationHistory.ts:43,49` | none found | Functional updater `setHistory(prev => [...prev, ...next.items])`; drop `history` dep |
| FE-03 | MEDIUM | 85 | Frontend | Blob preview URLs only revoked via explicit remove/clear | No unmount/navigation cleanup path | Memory growth in long sessions | `ui-next/src/stores/app-store.ts:101-129` | `clearAttachments()` in submit `finally` covers normal flow | Revoke on store-level cleanup/unload |
| FE-04 | HIGH | 95 (2nd-pass ✓) | Frontend | `readSSEStream` has no inactivity timeout; `reader.read()` blocks forever | `while(true)` with no watchdog | Dead backend ⇒ UI frozen "running" for minutes | `ui-next/src/lib/sse-reader.ts:12-55` (read at :24) | Abort signal works when caller aborts | Inactivity watchdog (~60s) that cancels the reader |
| FE-05 | MEDIUM | 85 | Frontend | `loadConversationsByThread` issues `getAll` without explicit transaction completion | Implicit idb transaction scope | Intermittent stale/partial results | `ui-next/src/lib/db.ts:71-75` | `loadConversationsPage` (87-109) does it correctly | Explicit `tx` + await pattern |
| FE-07 | MEDIUM | 78 | Frontend | Failed CSRF token refresh falls through silently, original 403 returned | Missing error branch when `getToken(true)` null | User stuck in broken state, no feedback | `ui-next/src/lib/security-client.ts:48-62` | 401 path handled explicitly | Throw/notify on refresh failure |
| FE-08 | MEDIUM | 82 | Frontend | Full markdown AST re-parse on every streamed chunk flush | `MarkdownRenderer` fed growing string | High CPU during synthesis streaming | `ui-next/src/app/chat/page.tsx:532-545` + ChatFeed render path | RAF buffering already throttles dispatch | Render plain text while streaming; markdown on completion |
| FE-09 | MEDIUM | 75 | Frontend | `WidgetRenderer` trusts `widget.result` shape; no per-widget error boundary | Unvalidated SSE payload into props | One malformed widget breaks message rendering (outer boundary hides whole feed) | `ui-next/src/components/widgets/WidgetRenderer.tsx:22-43` | `ChatErrorBoundary` exists but is feed-wide | Validate + per-widget try/fallback |
| FE-11 | HIGH | 90 (2nd-pass ✓) | Frontend | `handleResume` calls `readSSEStream(body, onResumeEvent)` with **no abort signal** | Missing AbortController in resume path | Resume events leak into a different conversation after switch | `ui-next/src/app/chat/page.tsx:955-958` | `startRun`/`startFollowup` correctly abort (usePipelineStream.ts:35,56) | Pass AbortController; abort on conversation switch/unmount |
| FE-12 | LOW | 70 | Frontend | `refresh()`/`save()` set no loading state | Missing state flag | Minor UX: stale list with no feedback | `ui-next/src/hooks/useConversationHistory.ts:51-70` | none found | Add `refreshing` state |
| XS-01 | MEDIUM | 100 (2nd-pass ✓) | Cross-system | `phase_warning` emitted by backend, declared in frontend types, never handled | Handler never implemented | Degraded-LLM warnings silently vanish | Emit: `api/streaming.py:200-207`; type: `ui-next/src/lib/types.ts:28`; no `case 'phase_warning'` in `chat/page.tsx` switch | none found | Add handler case surfacing warning in UI |
| XS-02 | MEDIUM | 90 | Cross-system | SSE `json_serializer` calls `.isoformat()` on naive datetimes without tz enforcement | No tz validation in serializer | Ambiguous timestamps cross-region | `api/sse_utils.py:23-26`; contrast `routes/health.py` UTC pattern | History timestamps (streaming.py:740) are tz-aware | Coerce naive→UTC in serializer |
| XS-03 | LOW | 85 (2nd-pass ✓) | Cross-system | `phase_retry.reason` sent by backend, missing from `PhaseEvent` type | Incomplete type contract | TS can't safely use `reason` | `api/streaming.py:648`; `ui-next/src/lib/types.ts:27-34` | none found | Add `reason?: string` |
| XS-04 | HIGH | 90 | Cross-system | No startup validation that Redis run-state works when `UVICORN_WORKERS>1` | Startup probes check rate-limiter Redis only | Cancellation/cleanup broken across workers, silently | `api/__init__.py:116-127` (probe scope); `redis/run_state.py:44-80`; cleanup path `api/streaming.py:815-821` | Single-worker default unaffected | Fail startup if multi-worker and run-state Redis unreachable |
| XS-05 | MEDIUM | 85 | Cross-system | SearXNG never health-checked; search errors swallowed to `[]` | No probe; silent catch | Web-search silently returns nothing; operator blind | `application/services/search_service.py:26-28`; `docker-compose.yml:130-148` (no healthcheck vs postgres/redis at 164/196) | none found | compose healthcheck + startup probe warning |
| XS-06 | LOW | 85 | Cross-system | `FollowupRequest.history: list[dict[str,str]]` vs frontend `ConversationTurn` with attachments | Schema narrower than actual payload | History attachments silently dropped in follow-ups | `api/schemas.py:164`; `ui-next/src/lib/types.ts:155-171`; use at `streaming.py:839-840` | none found | Widen schema; extract attachments |
| XS-07 | LOW | 92 | Cross-system | Cached replay skips the `connecting` event | Cache stored post-"connecting" | Minor UX inconsistency on cache hits | `api/streaming.py:336,911-912,935` | none found | Yield `connecting` before cache check |
| XS-09 | LOW | 88 | Reliability | WS broadcast failures logged but not counted | No metrics hook | No alertable signal for persistent WS loss | `api/sse_utils.py:31-59` (warn at :53) | none found | Failure counter/metric |
| XS-10 | LOW | 100 (2nd-pass ✓) | Deploy | `EXPOSE 8000` vs default `SERVER_PORT=8003` | Stale Dockerfile directive | Misleading container metadata | `Dockerfile:50`; `core/settings.py:96` | compose mapping can still be correct | `EXPOSE 8003` |
| XS-11 | LOW | 90 (2nd-pass ✓) | Deploy | `LOG_LEVEL` documented in `.env.example` but never read anywhere in `src/reasoner` | Stale doc or missing wiring | Operators can't tune verbosity via env | `.env.example:82`; grep of `src/reasoner` = 0 hits | none found | Wire into settings + logging init (or remove from example) |
| XS-13 | LOW | 78 | Cross-system | `FollowupRequest` lacks field validators beyond `question` | Validation asymmetry vs `RunRequest` | Invalid preset/conversation_id caught late | `api/schemas.py:154-181` vs 58-133 | Business logic catches downstream | Add validators |
| NEW-01 | INCONCLUSIVE | <60 | Infrastructure | Possible lock re-entrancy issue: `connect()` may invoke send path while holding `self._lock` which the send path also acquires | Unverified call-graph claim from second pass | Potential deadlock if real | `infrastructure/websocket/manager.py:112-118` vs 238-240 — **STATUS INCONCLUSIVE: requires call-graph trace before acting** | asyncio.Lock is not re-entrant, but the recursive call path was not conclusively demonstrated | Trace call graph during INF-03 fix; restructure if confirmed |
| TG-01…TG-10 | — | — | Testing | See `06_TESTING_GAPS.md` | — | — | — | — | — |
| TD-01…TD-06 | — | — | Tech debt | See `03/05` (clutter, shims, monoliths, lifespan coupling, healing tests unexecuted, TODO #501/#502) | — | — | TD-01: ~69 untracked root artifacts + 20 tracked legacy files (`git status`/`git ls-files`); TD-02: 30 shim files (~1,174 lines) at `src/reasoner/` root; TD-03: `preset_registry.py` 1987 / `pipeline_state.py` 1616 / `persuasion_defense.py` 1092 / `serializers.py` 1083 / `streaming.py` 941 lines; TD-04: `api/__init__.py` 789 lines; TD-05: `.github/workflows/self-healing-ci.yml:141-150` uploads generated tests but never runs them; TD-06: 7 TODOs all tied to #501/#502 tier lookup | — | See implementation plan |

## Refuted Findings (second-pass — do NOT implement)

| Original ID | Claim | Refutation Evidence |
|---|---|---|
| SEC-04 | Timing attack in API-key auth (plain `==`) | `infrastructure/auth_legacy.py:227` uses `secrets.compare_digest`; admin route likewise (`routes/admin.py:18-20`) |
| SEC-19 | `/api/calculate` RCE via eval | `widgets_legacy.py:127-170` uses AST parse + whitelist `_safe_eval_expr`; no eval/exec |
| SEC-01 (original framing) | Secrets "committed to version control", history scrub required | `git ls-files` tracks only `.env.example`; `git check-ignore .env` confirms ignored. Reframed as SEC-01R |
| FE-02 (original framing) | New runs don't abort previous SSE streams | `usePipelineStream.ts:35` aborts prior controller; `:56` passes signal. Residual real issue is the resume path = FE-11 |
| XS-12 | Phase-start models may include empty strings | `api/phase_executor.py:68` already truthiness-checks `provider.model` |
| BC-08 | `timed` decorator unawaited-coroutine risk | `PipelineState.log()` is synchronous; type-annotation nit only |
