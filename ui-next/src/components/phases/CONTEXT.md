# Context: Phases

## Directory: `ui-next/src/components/phases`

## Description
Exposes routing, templates, or integrations for Phases within the 'ui-next/src/components' ecosystem.

## Files
- **`ClassificationCard.tsx`**: Each task type gets a distinct, theme-safe treatment: text at the full
- **`CritiqueCard.tsx`**: eslint-disable-next-line @typescript-eslint/no-explicit-any
- **`PhaseCard.tsx`**: inside it) are set by whichever card owns the strip, so the same chip keeps its one-step lift on a --surface card and on a --surface-2 card without a tone prop threaded through every call site.
- **`PhaseRenderer.tsx`**: Code or resource asset facilitating system functionality.
- **`ResearchProgress.tsx`**: Code or resource asset facilitating system functionality.
- **`SourceCard.tsx`**: encoding that fails for the ~8% of readers who cannot separate amber from emerald — and for anyone printing the run. The glyph carries the type; the chip stays neutral and on-palette.
- **`SynthesisCard.tsx`**: `### Sources` heading the markdown renderer slugifies, but the synthesis path omits that section, so the chip pointed at nothing.
- **`SynthesisRenderer.tsx`**: Converts inline markdown links [title](url) into academic footnote-style citations. Footnote marks [^1], [^2]… appear inline; the reference list is appended at the very end of the document.

## Subfolders
*No subfolders in this directory.*
