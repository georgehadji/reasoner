# Context: Docs

## Directory: `ui-next/src/components/docs`

## Description
Exposes routing, templates, or integrations for Docs within the 'ui-next/src/components' ecosystem.

## Files
- **`DocMarkdown.tsx`**: Server-rendered markdown for documentation pages.  Deliberately not a client component: the prose must exist in the first HTML
- **`DocsSidebar.tsx`**: Documentation navigation. A server component, so every doc URL is a real anchor in the initial HTML and the whole set is discoverable in one crawl.

## Subfolders
*No subfolders in this directory.*
