# Context: Layout

## Directory: `ui-next/src/components/layout`

## Description
Global web shell layouts including navigational headers, user sidebars, and footer components.

## Files
- **`CommandPalette.tsx`**: Code or resource asset facilitating system functionality.
- **`Composer.tsx`**: active state is a shape (the leading dot), not only a hue, so it survives monochrome and colour blindness. `aria-pressed` carries it to AT.
- **`NeuroPanel.patterns.test.ts`**: Find the loadRecent function block
- **`NeuroPanel.tsx`**: Conversation ID to scope Neuro memory to the current thread
- **`PhaseTimeline.tsx`**: Code or resource asset facilitating system functionality.
- **`SecurityBadge.tsx`**: Code or resource asset facilitating system functionality.
- **`SecurityModal.tsx`**: Unmount when closed. `opacity-0 + pointer-events-none` hides it from sight
- **`ShortcutModal.tsx`**: Code or resource asset facilitating system functionality.
- **`Sidebar.tsx`**: 'loading' is distinct from 'unknown': the old initial value was 'unknown',
- **`SiteFooter.tsx`**: Code or resource asset facilitating system functionality.
- **`SiteHeader.tsx`**: `/docs/streaming` must light up the `/docs` tag, so match the prefix too.
- **`UpgradeModal.tsx`**: Code or resource asset facilitating system functionality.
- **`UsageBadge.tsx`**: Code or resource asset facilitating system functionality.
- **`UserMenu.tsx`**: users land at the top of the document after dismissing the popup.

## Subfolders
*No subfolders in this directory.*
