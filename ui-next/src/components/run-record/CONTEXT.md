# Context: Run-record

## Directory: `ui-next/src/components/run-record`

## Description
Exposes routing, templates, or integrations for Run-record within the 'ui-next/src/components' ecosystem.

## Files
- **`ApparatusToggle.tsx`**: The product's argument, made structurally rather than stated.  One paragraph of real output, rendered twice from the same parsed blocks.
- **`RunIndex.tsx`**: The record's table of contents, and the pipeline diagram, and the only timing chart on the page — one element doing all three jobs.
- **`RunRecord.tsx`**: This page is one real run of the product, laid out as the record that run produced.
- **`ScoreMatrix.tsx`**: The page's signature figure: the critique phase's actual score matrix.  A real `<table>`, not a picture of one — it keeps its meaning with CSS off,
- **`Segments.tsx`**: Renders one run of parsed inline segments.  Deliberately has no `'use client'`: the same renderer is used by the server

## Subfolders
*No subfolders in this directory.*
