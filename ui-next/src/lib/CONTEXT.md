# Context: Lib

## Directory: `ui-next/src/lib`

## Description
Surgical utility libraries for color schemes, API clients, and visual calculations.

## Files
- **`api-client.ts`**: Ask HyperGate which method it would pick, without running the pipeline.
- **`api-proxy.ts`**: Shared JSON proxy for backend routes the browser calls.  Every Next route handler that forwards to FastAPI needs the same five steps
- **`auth.ts`**: Supabase errors use either `message` or `msg`; `code` or `error_code`
- **`capabilities.generated.ts`**: AUTO-GENERATED by scripts/update_mindmap_meta.py on 2026-08-21 — do not edit by hand. Regenerated on every commit from live registry, preset, and phase counts (see tests/test_site_capabilities_sync.py) so marketing copy can
- **`changelog.ts`**: Changelog content.  A dated, moving changelog is one of the clearest "this is actively
- **`claims.test.ts`**: Guards against unbacked trust/compliance claims reappearing on public pages. Each phrase here was shipped at some point without evidence in the repo (no SOC 2 report, no BAA, no SAML implementation, no public MIT
- **`config.ts`**: Phase timeline labels per method — still used by PhaseTimeline component.
- **`conversation-history.test.ts`**: Code or resource asset facilitating system functionality.
- **`conversation-history.ts`**: Code or resource asset facilitating system functionality.
- **`db.test.ts`**: @vitest-environment jsdom
- **`db.ts`**: Code or resource asset facilitating system functionality.
- **`demo-record.test.ts`**: The home page states every one of its figures by counting this record. If the parser silently returns nothing the page still renders — just empty and wrong — so the check that matters is that the run actually parsed.
- **`demo-record.ts`**: The landing page's factual content: ONE real Reasoner run, parsed into the record structure the page renders.
- **`demo-run.json`**: Code or resource asset facilitating system functionality.
- **`docs.ts`**: Documentation content registry.  Docs are plain data rather than MDX so that every page is a static server
- **`error-reporting.ts`**: Report an error to both Sentry (if configured) and the backend error store. This ensures no user-facing error goes unlogged in production.
- **`faq.ts`**: FAQ content.  Shared by the FAQ page and its FAQPage structured data, so the answer a
- **`image-showcase.test.ts`**: The showcase is a real captured run, so the risk is drift rather than logic: a file gets moved or optimised away and the home page renders four broken images. These assert the two things that would actually break.
- **`image-showcase.ts`**: A real four-model image run, captured 2026-08-18 from the production code path (`generate_images(preset="budget", num_images=4)`).
- **`markdown.test.ts`**: Edge-case tests for markdown.ts — buildMarkdownFromPhase / buildMarkdownFromPhases.  Covers: all phase data formats, null/malformed data, XSS vectors,
- **`markdown.ts`**: eslint-disable @typescript-eslint/no-explicit-any
- **`method-hints.tsx`**: eslint-disable react/no-unescaped-entities
- **`provenance.ts`**: Mirrors the narrow-then-return-null shape of `getSynthesisSections` in `components/phases/PhaseRenderer.tsx` -- `data` is always `unknown` here
- **`route-suggestions.test.ts`**: The route table in `route-suggestions.ts` is a deliberate copy: importing the real sources would drag every doc body into the client bundle. These first two tests are what make the copy safe — they fail the moment a page is added
- **`route-suggestions.ts`**: Route recovery for the 404 page.  A 404 is a navigation failure, and the single most useful thing a 404 page
- **`schema.ts`**: schema.org builders for the site's structured data.  Kept in one module so every page describes the same organisation and product
- **`security-client.patterns.test.ts`**: Extract the block inside the 401 handler
- **`security-client.ts`**: Inject Supabase auth token if available
- **`security-constants.ts`**: Code or resource asset facilitating system functionality.
- **`security-csp.test.ts`**: Code or resource asset facilitating system functionality.
- **`security-csp.ts`**: Fall back to the explicit host/port allowlist above.
- **`security-server.edge-cases.test.ts`**: Security edge-case tests for server-side validation (security-server.ts).  Covers: validateRunRequest, validateRunFollowupRequest, validateCalculateRequest,
- **`security-server.test.ts`**: eslint-disable-next-line @typescript-eslint/no-explicit-any
- **`security-server.ts`**: Turbopack cache-bust hash — changing this forces a recompile of routes that import it.
- **`server-config.ts`**: Server-side configuration for Next.js API route proxies. Points to the Reasoner FastAPI backend.
- **`site.ts`**: Canonical site identity — the single source of truth for every absolute URL the app emits (metadata, sitemap, robots, JSON-LD, OpenGraph).
- **`sse-reader.ts`**: Read a Server-Sent Events stream from a ReadableStream.  @param stream - The ReadableStream to read from (e.g., response.body)
- **`supabase.ts`**: Code or resource asset facilitating system functionality.
- **`types.ts`**: phase_quality fields
- **`utils.ts`**: fallback below

## Subfolders
*No subfolders in this directory.*
