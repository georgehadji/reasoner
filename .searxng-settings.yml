# SearXNG settings for CI and local development
# Enables JSON search format required by integration tests

use_default_settings: true

server:
  secret_key: "reasoner-ci-secret-key-change-in-prod"
  limiter: false
  public_instance: false

search:
  formats:
    - html
    - json
