# Context: Test

## Directory: `sdk/typescript/test`

## Description
Unit and integration test suites for verifying the TypeScript client SDK's request and response handling.

## Files
- **`client.test.ts`**: An SSE response body built from event objects.
- **`contract.test.ts`**: The SDK half of the SSE contract.  `sdk/contract/events.json` is the shared source of truth. `tests/test_sdk_contract.py`
- **`errors.test.ts`**: Code or resource asset facilitating system functionality.
- **`sse.test.ts`**: Build a ReadableStream that emits the given strings as separate chunks.

## Subfolders
*No subfolders in this directory.*
